<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.usagiwisata02">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.UsagiWisata02">
        <activity android:name=".Profil"></activity>
        <activity
            android:name=".Streaming"
            android:label="@string/title_activity_streaming"
            android:theme="@style/Theme.UsagiWisata02.NoActionBar"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Walpaper"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".Home"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.UsagiWisata02.NoActionBar" />
        <activity
            android:name=".Splash_Screen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>